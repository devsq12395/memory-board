import React from 'react';
import MapComponent from "../components/common/MapComponent";

const Home: React.FC = () => {
  return (
    <div>
      <MapComponent />
    </div>
  );
};

export default Home;
